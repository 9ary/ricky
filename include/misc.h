#ifndef MISC_H
#define MISC_H

/*
 * Miscellaneous macros
 */

/*
 * Math
 */
#define min(a, b) (((a) < (b)) ? (a) : (b))
#define max(a, b) (((a) > (b)) ? (a) : (b))
#define in_range(x, a, b) (((x) >= (a)) && ((x) < (b)))

#endif
